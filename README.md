# SQL Practice Queries

This repository contains basic SQL queries created for practice and learning.

## Tasks Covered

The SQL script includes queries for:

- Selecting all columns from a table
- Selecting specific columns
- Aggregate functions (AVG, MIN, MAX)
- Selecting unique values (DISTINCT)
- Sorting and limiting results
- Counting rows in a value range
- Grouping data and calculating statistics per group
